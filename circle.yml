machine:
  java:
    version: oraclejdk8
  services:
    - docker
    
test:
  pre:
    - curl -fsSL get.nextflow.io | bash 
  override:
    - ./nextflow run . 
    - bash -x validate-ci.sh
    - ./nextflow run main-3.4.nf -profile gonl34
    
